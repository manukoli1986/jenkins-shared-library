#!/usr/bin/env groovy
@Library('jenkins-lib@master')
import io.abc.*

node('master'){
    stage('Clone'){
        git credentialsId: 'GithubCred', url: 'https://github.com/manukoli1986/simple-java-maven-app.git'
    }
    stage('Build'){
        sh 'jenkins-lib.mvnBuild'
        
    }
}
